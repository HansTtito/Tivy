% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{graficar_juveniles}
\alias{graficar_juveniles}
\title{Graficar porcentajes de juveniles (en número y peso)}
\usage{
graficar_juveniles(
  datos_juveniles,
  var_x,
  fill_var = NULL,
  tipo_grafico = "barras",
  titulo = NULL,
  limite_juv = NULL,
  ordenar_por = "x",
  paleta = NULL,
  facet_var = NULL,
  ncol = 2
)
}
\arguments{
\item{datos_juveniles}{Data frame con resultados de \code{juveniles_por_grupo}.}

\item{var_x}{Nombre de la variable a utilizar en el eje X del gráfico (por ejemplo, "fecha_unica").}

\item{fill_var}{(Opcional) Variable para agrupar y colorear las barras o líneas (por ejemplo, "embarcacion").}

\item{tipo_grafico}{Tipo de gráfico a mostrar: "barras" (por defecto), "lineas", "puntos", "mixto", o "boxplot".}

\item{titulo}{(Opcional) Título principal del gráfico.}

\item{limite_juv}{(Opcional) Valor umbral que representa el límite legal de porcentaje de juveniles; se añade como línea horizontal.}

\item{ordenar_por}{Criterio para ordenar el eje X: "x" (por defecto, orden según var_x), "numero", "peso" o "total".}

\item{paleta}{(Opcional) Vector de colores personalizados.}

\item{facet_var}{(Opcional) Variable para dividir el gráfico en subgráficos (facets).}

\item{ncol}{Número de columnas para el facetado (por defecto 2).}
}
\value{
Un objeto \code{ggplot} que representa el gráfico generado. Puede personalizarse o imprimirse directamente.
}
\description{
Genera visualizaciones para explorar los porcentajes de juveniles en función
de una variable seleccionada (por ejemplo, fecha o embarcación). Los datos deben
provenir de la función \code{juveniles_por_grupo}, que resume la proporción de juveniles
por grupo según talla y captura.
}
\details{
La función permite elegir entre diferentes tipos de gráficos (barras, líneas, puntos,
mixtos o boxplots) y personalizar opciones como colores, ordenamiento y facetado.
También es posible agregar una línea de referencia correspondiente al límite legal
de juveniles.
}
\examples{
# Procesamiento de datos
data_calas <- procesar_calas(data_calas = calas_bitacora)
data_faenas <- procesar_faenas(data_faenas = faenas_bitacora)
calas_tallas <- procesar_tallas(data_tallas = tallas_bitacora)
data_tallasfaenas <- merge(x = data_faenas, y = calas_tallas, by = "codigo_faena")
data_total <- merge_tallas_faenas_calas(data_calas = data_calas, data_tallas_faenas = data_tallasfaenas)

datos_final <- agregar_variables(data_total)
tallas_cols <- c("8", "8.5", "9", "9.5", "10", "10.5", "11", "11.5", "12",
                 "12.5", "13", "13.5", "14", "14.5", "15")
resultado <- ponderar_tallas_df(df = datos_final, tallas_cols = tallas_cols,
                                captura_col = "catch_ANCHOVETA", a = 0.0001, b = 2.984)
resultado$fecha_unica <- convertir_a_fecha(resultado$fecha_inicio_cala, tipo = "date")
resultado_juveniles <- juveniles_por_grupo(data = resultado,
                                           group_cols = c("fecha_unica"),
                                           cols_tallas = tallas_cols)

# Gráfico de barras de porcentajes de juveniles por fecha
graficar_juveniles(
  datos_juveniles = resultado_juveniles,
  var_x = "fecha_unica",
  limite_juv = 12
)
}
