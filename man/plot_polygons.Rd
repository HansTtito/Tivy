% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_polygons}
\alias{plot_polygons}
\title{Plot fishing suspension polygons}
\usage{
plot_polygons(
  data,
  coastline = NULL,
  parallels = NULL,
  type = "static",
  title = "Fishing suspension zones",
  colors = NULL,
  show_legend = FALSE,
  labels = NULL,
  add_grid = FALSE,
  base_layers = FALSE,
  minimap = FALSE
)
}
\arguments{
\item{data}{List of polygons with coordinates and metadata (such as announcement).}

\item{coastline}{\code{data.frame} with coastline coordinates. Must have columns named \code{Long} and \code{Lat}. If \code{NULL} (default), uses internal dataset \code{Tivy::peru_coastline}.}

\item{parallels}{A list containing data frames of lines parallel to the coast. If \code{NULL} (default), uses internal dataset \code{Tivy::peru_coast_parallels}.}

\item{type}{Type of plot to generate: \code{"static"} for ggplot2 or \code{"interactive"} for leaflet.}

\item{title}{Plot title.}

\item{colors}{Vector of colors to use to differentiate the polygons (by announcement or other category).}

\item{show_legend}{Logical. If \code{TRUE}, the legend is shown. Default is \code{FALSE}.}

\item{labels}{Optional vector of custom labels for each polygon. If not specified, the announcement name is used.}

\item{add_grid}{Logical. If \code{TRUE}, adds a reference grid to the static plot.}

\item{base_layers}{Logical. If \code{TRUE}, adds base layers (such as satellite or topography) to the interactive map.}

\item{minimap}{Logical. If \code{TRUE}, adds a minimap in the corner of the interactive map.}
}
\value{
An object of class \code{ggplot} if \code{type = "static"} or a \code{leaflet} object if \code{type = "interactive"}.
}
\description{
This function allows plotting fishing suspension polygons using
a base map of the Peruvian coast. Depending on the \code{type} parameter, it can
generate a static plot with \code{ggplot2} or an interactive plot with \code{leaflet}.
}
\examples{
# Create basic map

pdf_urls <- c(
  "https://consultasenlinea.produce.gob.pe/produce/descarga/comunicados/dgsfs/1542_comunicado1.pdf",
  "https://consultasenlinea.produce.gob.pe/produce/descarga/comunicados/dgsfs/1478_comunicado1.pdf",
  "https://consultasenlinea.produce.gob.pe/produce/descarga/comunicados/dgsfs/1468_comunicado1.pdf"
)

results <- extract_announcement_data(vector_pdf_names = pdf_urls)

plot_polygons(data = results)
}
