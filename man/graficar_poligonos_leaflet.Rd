% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial-functions.R
\name{graficar_poligonos_leaflet}
\alias{graficar_poligonos_leaflet}
\title{Graficar polígonos de zonas de suspensión pesquera con leaflet}
\usage{
graficar_poligonos_leaflet(
  datos,
  costa = Tivy::Shoreline_Peru,
  titulo = "Zonas de suspensión pesquera",
  colores = NULL,
  mostrar_leyenda = FALSE,
  etiquetas = NULL,
  capas_base = FALSE,
  minimap = FALSE
)
}
\arguments{
\item{datos}{Un data frame con coordenadas de latitud y longitud de inicio y fin,
que pueden estar en formato de texto (como "12°30'S") o decimales.}

\item{costa}{Un data frame que contiene la línea de costa para visualización.
Debe tener columnas 'Long' y 'Lat'. Por defecto utiliza Shoreline_Peru.}

\item{titulo}{Título para el gráfico.}

\item{colores}{Vector de colores para los polígonos.}

\item{mostrar_leyenda}{Lógico. Si es TRUE, muestra la leyenda. Default FALSE.}

\item{etiquetas}{Vector de textos para etiquetar cada polígono en la leyenda.}

\item{capas_base}{Lógico. Si es TRUE, agrega múltiples mapas base. Default FALSE.}

\item{minimap}{Lógico. Si es TRUE, agrega un minimapa al mapa interactivo. Default FALSE}
}
\value{
Un objeto leaflet que puede ser modificado con capas adicionales.
}
\description{
Crea visualizaciones interactivas de polígonos para zonas de suspensión pesquera
utilizando leaflet. Devuelve un objeto leaflet que puede ser modificado posteriormente.
}
\examples{
# Crear mapa básico
resultados <- extrae_data_comunicados(c("comunicado1.pdf"))
m <- graficar_poligonos_leaflet(datos = resultados, costa = Shoreline_Peru)

# Personalizar posteriormente
m \%>\%
  leaflet::addMarkers(lng = -77.1, lat = -12.0, popup = "Lima") \%>\%
  leaflet::addCircleMarkers(lng = -76.3, lat = -13.4, radius = 5, color = "red")

}
